server:
  port: 80

spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: localhost  #在linux的填linux的ip地址
      port: 8500
      discovery:
        service-name: ${spring.application.name}
