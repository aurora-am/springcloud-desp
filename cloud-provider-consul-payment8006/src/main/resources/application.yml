server:
  port: 8006

spring:
  application:
    name: consul-provider-payment
  cloud:
    consul:
      host: localhost  #在linux的填linux的ip地址
      port: 8500
      discovery:
        service-name: ${spring.application.name}
